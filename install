Для работы парсера файл parser.py 
В системе должна быть минимум версия python3.6(в идеале)
pip3.6 install --upgrade pip
pip3.6 install selenium-wire
pip3.6 install psycopg2
После установки необходимо в config.ini прописать настройки для подключения к бд, у пользователя БД должно быть максимум прав.